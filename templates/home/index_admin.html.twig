{% extends 'layout.html.twig' %}

{% block content %}
    {{ render(controller('App\\Controller\\WidgetController::header' )) }}
    <div class="page-content">
        {{ render(controller('App\\Controller\\WidgetController::adminMainSidebar')) }}
        <!-- Main content -->
        <div class="content-wrapper">

            <!-- Inner content -->
            <div class="content-inner">

                <div class="content">
                    {% if app.user.userType != constant('App\\Classes\\Data\\UserRolesData::ROLE_ADMIN') and app.user.userType != constant('App\\Classes\\Data\\UserRolesData::ROLE_SUPER_ADMIN') %}
                    <div class="row">
                        <div class="col-xl-4">
                            <div class="card">
                                <div class="card-header">
                                    <h5 class="mb-0">Lista zadataka</h5>
                                </div>

                                <div class="card-body">
                                    <p class="mb-3">Lista zadataka zadatih od strane administratora</p>

                                    <ul class="list list-unstyled mb-0">
                                        {% if checklist is not empty %}
                                            {% for task in checklist | slice(0,4) %}
                                                {% if task.status == 1  %}
                                                    <li>
                                                        <a href="{{ path('app_checklist_finish', {id: task.id}) }}"><i class="ph-x-circle text-danger me-2"></i></a>
                                                        {{ task.task }}
                                                    </li>
                                                {% elseif task.status == 2 %}
                                                    <li>
                                                        <i class="ph-check-circle text-success me-2"></i>
                                                        {{ task.task }}
                                                    </li>
                                                {% else %}
                                                    <li>
                                                        <del>{{ task.task }}</del>
                                                    </li>
                                                {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            Nemate zadatih zadataka
                                        {% endif %}
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    <div class="row d-none d-sm-block">
                        <div class="col-xl-12">
                            <!-- Marketing campaigns -->
                            <div class="card">

                                <div class="card-header d-lg-flex py-lg-0">
                                    <h5 class="py-lg-3 mb-0">Raspored za {{ danas | date('d.m.Y') }}</h5>
                                    <div class="mt-1 my-lg-auto ms-lg-auto">
{#                                        <a href="{{ path('app_task_form') }}" class="btn btn-primary"><i class="ph-plus me-2"></i> Dodajte</a>#}
                                        <a href="{{ path('app_quick_tasks') }}" class="btn btn-light">
                                            <i class="ph-clock-afternoon me-2"></i>
                                            Plan
                                        </a>
                                    </div>
                                </div>
                                <div class="table-responsive ">
                                    <table class="table table-hover table-bordered table-sm table-condensed">
                                        <thead>
                                        <tr class="table-light">
                                            <th colspan="7">PARS raspored</th>
                                        </tr>
                                        <tr>
                                            <th>Projekat</th>
                                            <th>Geodete</th>
{#                                            <th>Aktivnosti</th>#}
                                            <th>Oprema</th>
                                            <th>Vozilo</th>
                                            <th>Vreme</th>
                                            <th>Napomena</th>
                                            <th class="text-center" style="width: 20px;">
                                                <i class="ph-dots-three"></i>
                                            </th>
                                        </tr>
                                        </thead>
                                        <tbody>

                                        {% if timetable is not empty %}
                                            {% for task in timetable %}
                                                {% if task.task.project is not null %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::NIJE_ZAPOCETO') %}
                                                        <tr>
                                                    {% endif %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::ZAPOCETO') %}
                                                        <tr class="bg-danger bg-opacity-25">
                                                    {% endif %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::ZAVRSENO') %}
                                                        <tr class="bg-success bg-opacity-25">
                                                    {% endif %}
                                                        <td>
                                                            <div class="fw-semibold">
                                                                {{ task.task.project.title }}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            <div class="text-body">
                                                                {% for user in task.task.assignedUsers %}
                                                                    {{ user.fullName }} <br>
                                                                {% endfor %}
                                                            </div>
                                                        </td>
{#                                                        <td>#}
{#                                                            <div class="text-body">#}
{#                                                                {% for akt in task.task.activity %}#}
{#                                                                    {{ akt.title }} <br>#}
{#                                                                {% endfor %}#}
{#                                                            </div>#}
{#                                                        </td>#}
                                                        <td>
                                                            <div class="text-body">
                                                                {% for opt in task.task.oprema %}
                                                                    {{ opt.title }} <br>
                                                                {% endfor %}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            {% if task.car is not empty %}
                                                                <div class="text-body">
                                                                    {{ task.car[0].plate }}
                                                                </div>
                                                            {% endif %}
                                                        </td>
                                                        <td>
                                                            {% if task.task.time is not null %}
                                                                <div class="text-body">
                                                                    {{ task.task.time | date('H:i')}}h
                                                                </div>
                                                            {% endif %}
                                                        </td>
                                                        <td>
                                                            <div class="text-body">
                                                                {{ task.task.description }}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            <div class="text-body">
                                                                <a href="{{ path('app_task_view', {id:task.task.id}) }}" class="btn btn-primary"><i class="ph-clipboard me-2"></i> Dnevnik</a>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            <tr>
                                                <td colspan="7">
                                                    <div class="text-body">
                                                        Nema kreiranih zadataka za danas
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <!-- /marketing campaigns -->
                        </div>
                    </div>
                    <div class="row d-block d-sm-none">
                        <div class="col-xl-12">
                            <!-- Marketing campaigns -->
                            <div class="card">

                                <div class="card-header d-lg-flex py-lg-0">
                                    <h5 class="py-lg-3 mb-0">Raspored za {{ danas | date('d.m.Y') }}</h5>
                                    <div class="mt-1 my-lg-auto ms-lg-auto">
{#                                        <a href="{{ path('app_task_form') }}" class="btn btn-primary"><i class="ph-plus me-2"></i> Dodajte</a>#}
                                        <a href="{{ path('app_quick_tasks') }}" class="btn btn-light">
                                            <i class="ph-clock-afternoon me-2"></i>
                                            Plan
                                        </a>
                                    </div>
                                </div>
                                <div class="table-responsive ">
                                    <table class="table table-hover table-bordered table-sm table-condensed">
                                        <thead>
                                        <tr>
                                            <th>Projekat</th>
                                            <th>Geodete</th>
                                            <th class="text-center" style="width: 20px;">
                                                <i class="ph-dots-three"></i>
                                            </th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% if timetable is not empty %}
                                            {% for task in timetable %}
                                                {% if task.task.project is not null %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::NIJE_ZAPOCETO') %}
                                                        <tr>
                                                    {% endif %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::ZAPOCETO') %}
                                                        <tr class="bg-danger bg-opacity-25">
                                                    {% endif %}
                                                    {% if task.status == constant('App\\Classes\\Data\\TaskStatusData::ZAVRSENO') %}
                                                        <tr class="bg-success bg-opacity-25">
                                                    {% endif %}
                                                        <td style="width: 40%;">
                                                            <div class="fw-semibold">
                                                                <a href="{{ path('app_task_view', {id:task.task.id}) }}">{{ task.task.project.title }}</a> {% if task.task.time is not null %}({{ task.task.time | date('H:i')}}h){% endif %}
                                                            </div>
                                                        </td>
                                                        <td style="width: 50%;">
                                                            <div class="text-body">
                                                                {% for user in task.task.assignedUsers %}
                                                                    {{ user.getNameWithFirstLetter }} <br>
                                                                {% endfor %}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            <button data-bs-toggle="modal" data-bs-target="#modal_details_task{{ loop.index }}" class="btn btn-info"><i class="ph-info"></i></button>
                                                        </td>
                                                    </tr>
                                                {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            <tr>
                                                <td colspan="3">
                                                    <div class="text-body">
                                                        Nema kreiranih zadataka za danas
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <!-- /marketing campaigns -->
                        </div>
                    </div>

                    <div class="row d-none d-sm-block">
                        <div class="col-xl-12">
                            <!-- Marketing campaigns -->
                            <div class="card">

                                <div class="card-header d-lg-flex py-lg-0">
                                    <h5 class="py-lg-3 mb-0">Raspored za {{ sutra | date('d.m.Y') }}</h5>
                                    <div class="mt-1 my-lg-auto ms-lg-auto">
                                        <a href="{{ path('app_quick_task_edit', { id: tomorrowTimetableId}) }}" class="btn btn-primary"><i class="ph-eye me-2"></i> Vidite</a>
                                    </div>
                                </div>
                                <div class="table-responsive ">
                                    <table class="table table-hover table-bordered table-sm table-condensed">
                                        <thead>
                                        <tr class="table-light">
                                            <th colspan="6">PARS raspored</th>
                                        </tr>
                                        <tr>
                                            <th>Projekat</th>
                                            <th>Geodete</th>
{#                                            <th>Aktivnosti</th>#}
                                            <th>Oprema</th>
                                            <th>Vozilo</th>
                                            <th>Vreme</th>
                                            <th>Napomena</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% if tomorrowTimetable is not empty %}
                                            {% for task in tomorrowTimetable %}
                                                {% if task['projekat'] is not null %}
                                                    <tr>
                                                        <td>
                                                            <div class="fw-semibold">
                                                                {{ task['projekat'].title }}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            <div class="text-body">
                                                                {% if task['geo1'] is not null %}
                                                                    {{ task['geo1'].fullName }} <br>
                                                                {% endif %}
                                                                {% if task['geo2'] is not null %}
                                                                    {{ task['geo2'].fullName }} <br>
                                                                {% endif %}
                                                                {% if task['geo3'] is not null %}
                                                                    {{ task['geo3'].fullName }}
                                                                {% endif %}
                                                            </div>
                                                        </td>
{#                                                        <td>#}
{#                                                            <div class="text-body">#}
{#                                                                {% for akt in task['aktivnosti'] %}#}
{#                                                                    {{ akt.title }} <br>#}
{#                                                                {% endfor %}#}
{#                                                            </div>#}
{#                                                        </td>#}
                                                        <td>
                                                            <div class="text-body">

                                                                    {% for opr in task['oprema'] %}
                                                                        {{ opr.title }} <br>
                                                                    {% endfor %}

                                                            </div>
                                                        </td>
                                                        <td>
                                                            {% if task.vozilo is not null %}
                                                                <div class="d-inline-flex align-items-center">
                                                                    {{ task.vozilo.plate }}
                                                                </div>
                                                            {% endif %}
                                                        </td>
                                                        <td>
                                                            {% if task.vreme is not null %}
                                                                <div class="d-inline-flex align-items-center">
                                                                    {{ task.vreme}}h
                                                                </div>
                                                            {% endif %}
                                                        </td>
                                                        <td>
                                                            <div class="text-body">
                                                                {{ task['napomena'] }}
                                                            </div>
                                                        </td>
                                                    </tr>
                                                {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            <tr>
                                                <td colspan="6">
                                                    <div class="text-body">
                                                        Nema kreiranih zadataka za sutra
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <!-- /marketing campaigns -->
                        </div>
                    </div>
                    <div class="row d-block d-sm-none">
                        <div class="col-xl-12">
                            <!-- Marketing campaigns -->
                            <div class="card">

                                <div class="card-header d-lg-flex py-lg-0">
                                    <h5 class="py-lg-3 mb-0">Raspored za {{ sutra | date('d.m.Y') }}</h5>
                                    <div class="mt-1 my-lg-auto ms-lg-auto">
                                        <a href="{{ path('app_quick_task_edit', { id: tomorrowTimetableId}) }}" class="btn btn-primary"><i class="ph-eye me-2"></i> Vidite</a>
                                    </div>
                                </div>
                                <div class="table-responsive ">
                                    <table class="table table-hover table-bordered table-sm table-condensed">
                                        <thead>
                                        <tr>
                                            <th>Projekat</th>
                                            <th>Geodete</th>
                                            <th class="text-center" style="width: 20px;">
                                                <i class="ph-dots-three"></i>
                                            </th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% if tomorrowTimetable is not empty %}
                                            {% for task in tomorrowTimetable %}
                                                {% if task['projekat'] is not null %}
                                                    <tr>
                                                        <td style="width: 40%;">
                                                            <div class="fw-semibold">
                                                                {{ task['projekat'].title }} {% if task.vreme is not null %} ({{ task.vreme}}) {% endif %}
                                                            </div>
                                                        </td>
                                                        <td style="width: 50%;">
                                                            <div class="text-body">
                                                                {% if task['geo1'] is not null %}
                                                                    {{ task['geo1'].getNameWithFirstLetter }} <br>
                                                                {% endif %}
                                                                {% if task['geo2'] is not null %}
                                                                    {{ task['geo2'].getNameWithFirstLetter }} <br>
                                                                {% endif %}
                                                                {% if task['geo3'] is not null %}
                                                                    {{ task['geo3'].getNameWithFirstLetter }}
                                                                {% endif %}
                                                            </div>
                                                        </td>
                                                        <td>
                                                            <button data-bs-toggle="modal" data-bs-target="#modal_details1_task{{ loop.index }}" class="btn btn-info"><i class="ph-info"></i></button>
                                                        </td>
                                                    </tr>
                                                {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            <tr>
                                                <td colspan="3">
                                                    <div class="text-body">
                                                        Nema kreiranih zadataka za sutra
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            <!-- /marketing campaigns -->
                        </div>
                    </div>
                </div>
                <!-- /content area -->

                <!-- Footer -->
                {% include 'includes/footer.html.twig' %}
                <!-- /footer -->

            </div>
            <!-- /inner content -->

        </div>
        <!-- /main content -->
        {{ render(controller('App\\Controller\\WidgetController::rightSidebar')) }}
    </div>
    {% include 'includes/notification.html.twig' %}
    {% if tomorrowTimetable is not empty %}
        {% for task in tomorrowTimetable %}
            {% if task['projekat'] is not null %}
                <div id="modal_details1_task{{ loop.index }}" class="modal fade" tabindex="-1">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header bg-info text-white border-0">
                                <h6 class="modal-title">Detalji zadatka</h6>
                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                            </div>

                            <div class="modal-body">
                                <div class="card">
                                    <div class="card-body">
                                        <div class="row p-2">
                                            <div class="col-4">Projekat:</div>
                                            <div class="col-8">{{ task['projekat'].title }}</div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Geodete:</div>
                                            <div class="col-8">
                                                {% if task['geo1'] is not null %}
                                                    {{ task['geo1'].getNameWithFirstLetter }} <br>
                                                {% endif %}
                                                {% if task['geo2'] is not null %}
                                                    {{ task['geo2'].getNameWithFirstLetter }} <br>
                                                {% endif %}
                                                {% if task['geo3'] is not null %}
                                                    {{ task['geo3'].getNameWithFirstLetter }}
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Aktivnosti:</div>
                                            <div class="col-8">
                                                {% for akt in task['aktivnosti'] %}
                                                    {{ akt.title }} <br>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Oprema:</div>
                                            <div class="col-8">

                                                {% for opr in task['oprema'] %}
                                                    {{ opr.title }} <br>
                                                {% endfor %}

                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Vozilo:</div>
                                            <div class="col-8">
                                                {% if task.vozilo is not null %}
                                                    {{ task.vozilo.getCarName }}
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Vreme:</div>
                                            <div class="col-8">
                                                {% if task.vreme is not null %}
                                                    {{ task.vreme}}
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Napomena:</div>
                                            <div class="col-8">{{ task.napomena }}</div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="modal-footer">
                                <button type="button" class="btn btn-outline-info" data-bs-dismiss="modal"><i class="ph-x me-2"></i> Zatvorite</button>
                            </div>
                        </div>
                    </div>
                </div>
            {% endif %}
        {% endfor %}
    {% endif %}
    {% if timetable is not empty %}
        {% for task in timetable %}
                <div id="modal_details_task{{ loop.index }}" class="modal fade" tabindex="-1">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header bg-info text-white border-0">
                                <h6 class="modal-title">Detalji zadatka</h6>
                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                            </div>

                            <div class="modal-body">
                                <div class="card">
                                    <div class="card-body">
                                        <div class="row p-2">
                                            <div class="col-4">Projekat:</div>
                                            <div class="col-8">{{ task.task.title }}</div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Geodete:</div>
                                            <div class="col-8">
                                                {% for user in task.task.assignedUsers %}
                                                    {{ user.getNameWithFirstLetter }} <br>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Aktivnosti:</div>
                                            <div class="col-8">
                                                {% for akt in task.task.activity %}
                                                    {{ akt.title }} <br>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Oprema:</div>
                                            <div class="col-8">
                                                {% for opt in task.task.oprema %}
                                                    {{ opt.title }} <br>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Vozilo:</div>
                                            <div class="col-8">
                                                {% if task.car is not empty %}
                                                    {{ task.car[0].plate }}
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="row p-2 bg-light">
                                            <div class="col-4">Vreme:</div>
                                            <div class="col-8">
                                                {% if task.task.time is not null %}
                                                    {{ task.task.time | date('H:i')}}h
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="row p-2">
                                            <div class="col-4">Napomena:</div>
                                            <div class="col-8">{{ task.task.description }}</div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="modal-footer">
                                <button type="button" class="btn btn-outline-info" data-bs-dismiss="modal"><i class="ph-x me-2"></i> Zatvorite</button>
                            </div>
                        </div>
                    </div>
                </div>
        {% endfor %}
    {% endif %}
{% endblock content %}

{% block includeJavascript %}
    {{ parent() }}
    <script src="{{ asset('assets/demo/pages/dashboard.js') }}"></script>
    <script src="{{ asset('assets/demo/pages/task_manager_detailed.js') }}"></script>
    <script src="{{ asset('assets/demo/pages/components_modals.js') }}"></script>

{% endblock includeJavascript %}